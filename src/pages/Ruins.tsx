export default function Ruins() {
    return (
        <section className="relative h-screen w-full overflow-hidden bg-black">
            {/* 보스 컷신 캔버스/이미지 자리 */}
            <h1 className="absolute inset-0 flex items-center justify-center text-6xl text-white">
                Ruins
            </h1>
        </section>
    );
}
